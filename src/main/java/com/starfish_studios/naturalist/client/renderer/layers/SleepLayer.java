package com.starfish_studios.naturalist.client.renderer.layers;

import net.fabricmc.api.EnvType;
import net.fabricmc.api.Environment;
import net.minecraft.resources.ResourceLocation;

@Environment(EnvType.CLIENT)
public class SleepLayer<T> {
    // Dummy class to resolve compilation errors
    public SleepLayer(Object renderer, ResourceLocation model, ResourceLocation layer) {
    }
}
